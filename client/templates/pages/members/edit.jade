section.page.edit-member
  
  h2 Edit Person

  p This form and all behavior is defined by the form view in <code>client/forms/member.js</code>.

  p The same form-view is used for both editing and creating new users.

  form(data-hook="member-form")
    fieldset(data-hook="field-container")
    .buttons
      button.ink-button(data-hook="reset", type="submit") Submit
